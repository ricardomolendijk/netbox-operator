# Generated by hack/credential-rbac.sh from namespaces.txt. DO NOT EDIT.
#
# One Role per namespace holding endpoint credential Secrets, replacing the
# cluster-wide 'secrets' rule the generated ClusterRole used to carry (NBO-072).
#
# All three verbs are load-bearing. The informer LISTs a namespace and then WATCHes
# it, and RBAC evaluates a namespaced watch against the Role in that namespace, so
# 'watch' here is what makes a rotated token take effect without a restart. 'get' is
# not issued in normal operation -- reads are served from the informer -- and is kept
# because it is strictly weaker than 'list' and it is the verb
# 'kubectl auth can-i get secrets -n <ns>' asks about.
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: netbox-operator-endpoint-credential-reader
  namespace: default
  labels:
    app.kubernetes.io/name: netbox-operator
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: netbox-operator-endpoint-credential-reader
  namespace: default
  labels:
    app.kubernetes.io/name: netbox-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: netbox-operator-endpoint-credential-reader
subjects:
- kind: ServiceAccount
  name: netbox-operator-controller-manager
  namespace: netbox-operator-system
