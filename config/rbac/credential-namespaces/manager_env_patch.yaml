# Generated by hack/credential-rbac.sh from namespaces.txt. DO NOT EDIT.
#
# The namespaces the manager builds Secret informers for. It must match the Roles in
# rbac.yaml: a cluster-scoped informer would issue a cluster-scoped LIST and WATCH,
# which no Role can authorise, and the manager would never sync.
#
# The Deployment is named before config/base's namePrefix and namespace
# transformers, which is how kustomize matches a component's patch to it.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        env:
        - name: NETBOX_CREDENTIAL_NAMESPACES
          value: default
