netbox-operator {{ .Chart.AppVersion }} is installed in namespace {{ .Release.Namespace }}.

  kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ include "netbox-operator.fullname" . }}

Secret access is namespaced. The operator may read Secrets labelled
netbox.kubeforge.org/endpoint-credential=true in exactly these namespaces:

  {{ include "netbox-operator.credentialNamespaces" . }}

An endpoint anywhere else reports Ready=False/SecretMissing. Widen it with
--set credentialNamespaces={a,b} and note that a chart upgrade does not delete the Roles
it no longer renders (docs/operations/rbac.md).

CRDs: this chart does not contain them and Helm does not manage them (#265 -- 2.7 MB of
CRDs put the release Secret over the API server's 1 MiB cap and failed every install). You
applied them before this install; apply them again after every upgrade, before the upgrade:

  make upgrade-crds        # from a checkout

  kubectl apply --server-side --force-conflicts -f \
    https://github.com/ricardomolendijk/netbox-operator/releases/download/v{{ .Chart.Version }}/netbox-operator-crds-{{ .Chart.Version }}.yaml

CRDs first, because a manager that reconciles a field the old CRD prunes fails in a way that
looks like a bug in the operator. Nothing deletes them -- deleting a CRD deletes every CR of
that kind and their finalizers would then delete the NetBox objects. docs/install.md.
{{- if .Values.metrics.enabled }}

Metrics: {{ if .Values.metrics.secure }}https{{ else }}http{{ end }}://{{ include "netbox-operator.fullname" . }}-metrics.{{ .Release.Namespace }}:{{ .Values.metrics.port }}/metrics
{{- if .Values.metrics.secure }} (self-signed certificate, no authn filter -- a scraper
needs insecureSkipVerify and the port must not leave the cluster).
{{- end }}
{{- if and .Values.metrics.serviceMonitor.enabled (not (.Capabilities.APIVersions.Has "monitoring.coreos.com/v1")) }}

WARNING: metrics.serviceMonitor.enabled is set but monitoring.coreos.com/v1 is not
installed on this cluster, so no ServiceMonitor was created.
{{- end }}
{{- end }}
{{- if include "netbox-operator.webhookEnabled" . }}

Admission: the validating webhook is registered, and cert-manager issues its serving
certificate into Secret {{ include "netbox-operator.webhookCertName" . }}. The manager's Pod
stays in ContainerCreating until that Secret exists, which is seconds on a healthy cluster
and forever if cert-manager is installed but not running.
{{- else if .Values.webhook.enabled }}

WARNING: cert-manager.io/v1 is not installed, so no validating webhook was created and the
manager runs with --enable-webhooks=false. Three denials and three warnings move from apply
time to reconcile time, where each of them has a backstop -- nothing is silently accepted and
no NetBox write happens on a blocked object
(docs/operations/admission-webhooks.md#what-breaks-when-it-is-off).

Install cert-manager and re-run `helm upgrade` to turn admission on, or set
webhook.certManager.required=true to make its absence an install-time error instead.
{{- end }}
{{- if not .Values.endpoint.enabled }}

No NetBoxEndpoint was created. Apply one (docs/examples/) or set endpoint.enabled=true.
{{- end }}
{{- if eq .Values.drift.mode "Report" }}

drift.mode is Report: the endpoint this chart renders writes NOTHING to NetBox and no
object reaches Ready=True. That is migration time, not an operating mode -- move to
Correct once you have read the drift it reports.
{{- end }}
