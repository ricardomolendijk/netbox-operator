# 0005 — Coexisting with Flux and Argo CD

**Status:** Accepted · 2026-08-21

## The problem

This operator reconciles **outward**, from Kubernetes to NetBox. Flux and Argo CD
reconcile **inward**, from Git to Kubernetes. The CRs are in the middle, and if both
sides write them, they fight forever — each one "correcting drift" the other just
introduced, at whatever the shorter of the two resync intervals is.

Worth being precise about which interactions are actually a problem, because most are
not:

| Interaction | Problem? |
|---|---|
| Human edits NetBox; operator corrects it back to match the CR | **No.** That is drift correction and it is the entire point. Git is untouched. |
| Git changes; Flux/Argo updates the CR; operator pushes it to NetBox | **No.** That is the intended flow. |
| Operator writes to a CR's **`status`** | **No.** Flux ignores `status`; Argo CD ignores it for CRs by default. |
| Operator writes to a CR's **`spec`** | **Yes — this is the fight.** The GitOps tool sees the live spec diverge from Git and reverts it. The operator writes it again. Loop. |
| Operator **creates** a CR that is not in Git (a materialised child, a claim's `NetBoxIPAddress`) | **Sometimes.** Argo CD flags it `OutOfSync` as an extraneous resource unless told not to. Flux prunes by inventory and ignores it. |
| Git deletes a CR; Flux/Argo prunes it; finalizer deletes the NetBox object | **No** — and it is the desired behaviour. |

So the fix is one hard invariant plus some ergonomics.

## Decision

### 1. The operator never writes to `spec`. Ever.

All operator output goes to `status` — allocated addresses, resolved NetBox IDs, URLs,
conditions, observed generation. There is no code path that mutates the spec of an
object the operator did not itself create, and none at all that mutates a spec field a
user or Git could have set.

This is the whole ballgame. Enforced, not merely intended:

- The manager's client is built with the CR types' `spec` writes going through
  `Status().Update()` / `Status().Patch()` only.
- A unit test asserts, for every registered kind, that a reconcile of an unchanged
  object issues zero non-status writes.
- `envtest` coverage asserts `metadata.generation` is unchanged across a reconcile that
  only updates status — a generation bump is the signature of a spec write.

Consequence to accept: **an allocated address lives in `status`, not in Git.** See §3.

### 2. Objects the operator creates are labelled as not-Git's

Every CR the operator materialises — inline children (NBO-032), a claim's resulting
resource (NBO-036) — carries, in addition to its owner reference:

```yaml
metadata:
  labels:
    app.kubernetes.io/managed-by: netbox-operator
  annotations:
    argocd.argoproj.io/compare-options: IgnoreExtraneous
    netbox.populator.io/generated-by: netboxipaddressclaim/homelab/dns-eth0
```

`IgnoreExtraneous` is Argo CD's own mechanism for exactly this case: a resource
generated by a controller rather than by the manifest set. Without it, an Argo
Application containing a `NetBoxVirtualMachine` with inline interfaces reports
`OutOfSync` forever, because the interfaces exist in the cluster and not in Git.

The annotation set is **configurable, and every entry can be disabled**, because
annotating for a tool you do not run is noise. See §5.

### 3. Allocations survive a cluster rebuild, without writing to Git

The obvious objection to "allocations live in `status`" is: rebuild the cluster from
Git and every claim re-allocates, handing out different addresses.

Fixed by making the idempotency key **deterministic** rather than random:

```
netbox.populator.io/allocation-identity = sha256(endpoint.url + namespace + kind + name)[:16]
```

written to a NetBox custom field (or a tag, if custom fields are unavailable) on the
allocated object. Before allocating, the claim controller **searches NetBox for its own
identity** and adopts the existing object if it finds one.

So a claim that is deleted and re-created from the same Git manifest — same namespace,
same name — reclaims **the same address**. Rebuilding a cluster from Git converges back
to the state NetBox is already in, and NetBox itself is the durable store, which is
where the source of truth for an allocated address arguably belongs anyway.

`spec.allocationIdentity` overrides the derived value, for the case where a claim is
renamed but should keep its address.

This supersedes the "write the claim's UID" mechanism in
[ADR-0004](0004-claims-first-allocation.md): a UID is regenerated when an object is
re-created, which is precisely the case that needs to survive. The UID is still recorded
in `status` for debugging, and still used to detect "a *different* claim now has this
name".

### 4. No Git write-back in core

Writing the allocated address back to Git — opening a commit or a PR that pins
`status.address` into the manifest — is deliberately **not** implemented. Reasons, in
order of weight:

1. **§3 makes it unnecessary** for the problem it is usually reached for. Allocations are
   already stable across rebuilds.
2. It requires the operator to hold **write credentials to the repository**, which is a
   substantially larger security surface than a NetBox token, and turns a compromised
   operator into a supply-chain problem.
3. It creates a **second writer to Git**, with all the merge-conflict and
   PR-noise handling that implies (Flux's image-automation controller is an existence
   proof that this is a whole subsystem, not a feature).
4. It inverts the model: if Git is meant to contain the address, the user should be
   using `NetBoxIPAddress` with an explicit address, not a claim. The claim exists
   precisely to say "I don't want to know."

Reconsidered only if §3 proves insufficient in practice. Tracked as an open decision.

### 5. All of it is configurable, and the Helm chart is where

Nothing above is compiled in. Chart values (NBO-061):

```yaml
gitops:
  # Annotations applied to CRs the operator generates. Disable what you don't run.
  argocd:
    enabled: true            # argocd.argoproj.io/compare-options: IgnoreExtraneous
  flux:
    enabled: false           # kustomize.toolkit.fluxcd.io/reconcile: disabled
  extraAnnotations: {}

drift:
  # How aggressively NetBox-side drift is corrected.
  mode: Correct             # Correct | Report | Off
  resyncPeriod: 10m         # periodic re-check even with no CR change
  webhookReceiver:
    enabled: false          # NBO-045: sub-5s correction driven by NetBox events

allocation:
  identity:
    mode: Derived           # Derived | Explicit — see §3
    customField: k8s_allocation_identity
```

`drift.mode: Report` is the one worth calling out: it makes the operator observe and
report NetBox-side drift via conditions, Events and metrics **without correcting it**.
That is the mode for running this alongside a team that still edits NetBox by hand, and
for the first week of any adoption.

## Docs impact

`docs/operations/gitops.md` — a page per tool, with the Argo CD `Application` and Flux
`Kustomization` snippets that make it quiet, the `ignoreDifferences` block for
`status`, and an explicit walkthrough of the cluster-rebuild case.
